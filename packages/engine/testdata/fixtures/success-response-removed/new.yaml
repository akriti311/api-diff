openapi: 3.0.3
info:
  title: Items API
  version: 1.1.0
paths:
  /items/{id}:
    get:
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "404":
          description: missing
