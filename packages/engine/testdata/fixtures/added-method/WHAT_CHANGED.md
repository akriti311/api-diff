# Added method

The path `/users/{id}` already existed. **POST** was added.

**Later classification:** non-breaking (old clients still use GET).

This is a **method** add, not a new endpoint, because the path was already there.
