# Same change, JSON instead of YAML

This is the hero example again (`name` removed), written as `.json`.

OpenAPI is the same contract either way. In Phase 2 the parser will accept both. The comparison engine must not care which file format was used.
