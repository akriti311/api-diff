openapi: 3.0.3
info:
  title: Orders API
  version: 1.1.0
paths:
  /orders:
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
                - userId
              properties:
                email:
                  type: string
                userId:
                  type: string
      responses:
        "201":
          description: created
